{
  "name": "voxcoach",
  "version": "1.0.0",
  "description": "VOXCOACH — AI-Powered Public Speaking Coach (Learn. Speak. Improve.)",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "build": "npm run build:server && npm run build:client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "postinstall": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "keywords": [
    "public-speaking",
    "audio-analysis",
    "ai-coach",
    "speech-to-text",
    "pitch-detection",
    "gemini",
    "react",
    "express"
  ],
  "author": "VoxCoach Team",
  "license": "MIT"
}
